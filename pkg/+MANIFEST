name = "vauban";
version = "%%VERSION%%";
origin = "security/vauban";
comment = "Security bastion with SSH and RDP recording, privsep and capsicum(4) sandboxing";
maintainer = "none@freebsd.org";
www = "https://vauban.sh";
prefix = "/usr/local";
licenselogic = "single";
licenses = ["BSD2CLAUSE"];
categories = ["security"];

desc = <<EOD
VAUBAN is a security bastion software providing:
 - Privileged access management (PAM)
 - SSH terminal proxying with asciicast v2 session recording and password redaction
 - RDP desktop proxying with segmented fMP4 recording and DASH playback
 - Role-based access control (RBAC)
 - Multi-factor authentication (TOTP)
 - Centralized audit logging
 - Hardened secrets vault with fine-grained access control
 - ACME certificate auto-renewal (Let's Encrypt / ZeroSSL)
 - Defense in depth with privsep and capsicum(4) sandboxing
 - Post-quantum cryptography enforced
EOD;

deps = {
    postgresql18-server = {
        origin = "databases/postgresql18-server";
        version = "18";
    };
    postgresql18-contrib = {
        origin = "databases/postgresql18-contrib";
        version = "18";
    };
};
